#include "holberton.h"
#include <stdio.h>

/**
 * main - prints the name of the program
 * @argc: count the size of argv
 * @argv: array that contains the arguments
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
